# Archivos estáticos

Coloca aquí para que se sirvan en la raíz:

- `logo_loeram_short.png` — logo del encabezado (o copia desde `src/assets/`)
- `logo_loeram_favicon.png` — favicon de la pestaña (o copia desde `src/assets/`)
